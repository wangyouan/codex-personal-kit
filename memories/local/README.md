# Local Memory

This folder is for templates only.

Real local memory should stay on each computer, for example:

```text
$env:USERPROFILE\.codex\memories\local\
```

The repository ignores files in this folder so each computer can have its own notes without Git conflicts.
